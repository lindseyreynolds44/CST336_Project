/* global $ */
$(document).ready(function () {
  /**
   * Search button action event
   */
  // Request this movie using an AJAX call to route: "/search"
  // Return value: resultArray 
  // Attributes: title, imageUrl, rating, movieID,
  // release_date, overview, genres
  // EX: resultArray.title

  /**
   * Create new account action event
   */
  // Add new user and password using AJAX to route: "/createAccount"
  // Parameters: firstName, lastName, username and password
  
  /**
   * Add movie to Cart action event
   */
  // Use AJAX to send movie to the database using route: "/updateCart"
  // Parameters: movie_id and action: "add"
  
  /**
   * Remove movie from Cart action event
   */
  // Use AJAX to remove movie from the database using route: "/updateCart"
  // Parameters: movie_id and action: "delete"
  
  /**
   * Displays the stored contents of the cart from DB.
   * */
  // Use ajax to request contents from cart.
  // Use ajax response to render cart modal using
  // jquery to render the modal "onClick"
});
